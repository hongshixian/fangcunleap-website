FROM node:24-alpine

# 设置工作目录
WORKDIR /app

# 安装依赖（仅复制 package 文件）
COPY package*.json ./
COPY pnpm-lock.yaml ./

RUN npm ci --legacy-peer-deps

# 暴露开发服务器端口
EXPOSE 3000

# 启动开发服务器
CMD ["npm", "run", "dev"]
